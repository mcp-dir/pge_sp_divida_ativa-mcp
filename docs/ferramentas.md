# Ferramentas

Procuradoria Geral do Estado SP: Dívida Ativa expõe 1 ferramenta (todas somente leitura).

### 1. `pge_sp_divida_ativa_consultar`
**Input**: `cda` (opcional), `cnpj` (opcional), `cnpj_base` (opcional), `cpf` (opcional), `ie` (opcional), `renavam` (opcional)

Procuradoria Geral do Estado SP: Dívida Ativa, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Procuradoria Geral do Estado SP: Dívida Ativa
```
